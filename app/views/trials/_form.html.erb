<%= form_for @trial, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :trl_code, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :trl_code, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :trl_name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :trl_name, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                trials_path, :class => 'btn' %>
  </div>
<% end %>
